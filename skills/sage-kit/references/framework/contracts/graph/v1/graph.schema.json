{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:sagekit:graph-contract:v1:graph",
  "title": "SAGE-Kit Graph Contract v1",
  "description": "A language-neutral structural data boundary. Schema presence does not activate Graph execution, create runtime state or a scheduler, modify ACTIVE_CONTEXT, execute nodes, or change project authority. Light governance remains Graph-optional. JSON Schema checks local shape only; reference integrity, cycles, reachability, join safety, and external-gate topology require separate consumer-selected validation or documented model review.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schema_id",
    "schema_version",
    "graph_id",
    "generation",
    "source_authority",
    "governance_level",
    "autonomy_level",
    "human_gates",
    "nodes",
    "joins"
  ],
  "properties": {
    "schema_id": {
      "const": "urn:sagekit:graph-contract:v1:graph",
      "description": "Stable identity of the Graph Contract schema used by this instance."
    },
    "schema_version": {
      "const": 1,
      "description": "Version of the Graph Contract instance boundary."
    },
    "graph_id": {
      "type": "string",
      "minLength": 1,
      "description": "Authority-scoped identity of this graph instance."
    },
    "generation": {
      "type": "integer",
      "minimum": 1,
      "description": "Declared graph generation; it does not authorize graph rewriting."
    },
    "source_authority": {
      "$ref": "#/$defs/source_authority"
    },
    "governance_level": {
      "type": "string",
      "enum": [
        "Light",
        "Standard",
        "Heavy"
      ],
      "description": "The externally selected control structure. Governance is independent from permission and autonomy and grants neither."
    },
    "autonomy_level": {
      "type": "string",
      "enum": [
        "turn-based",
        "goal-based"
      ],
      "description": "The host continuation mode, independent from governance. goal-based records that a host may continue only while an external completion verifier, authority envelope, and convergence rule permit it; this field does not grant continuation authority."
    },
    "completion_verifier": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048,
      "description": "External completion verifier reference. It is required for goal-based autonomy, but the schema does not execute it, establish that verification occurred, or treat it as authority."
    },
    "continuation_envelope": {
      "$ref": "#/$defs/continuation_envelope_reference",
      "description": "External bounded-continuation envelope references. It is required for goal-based autonomy; recording it does not admit work, permit continuation, or grant authority."
    },
    "human_gates": {
      "type": "array",
      "items": {
        "type": "string",
        "minLength": 1
      },
      "uniqueItems": true,
      "description": "Human gate identities or references. Recording a gate cannot open, close, waive, or reclassify it."
    },
    "nodes": {
      "type": "array",
      "minItems": 1,
      "items": {
        "$ref": "#/$defs/node"
      },
      "description": "Declared nodes only; presence does not create runtime state, scheduling, or execution."
    },
    "joins": {
      "type": "array",
      "items": {
        "$ref": "#/$defs/join"
      },
      "description": "Declared join boundaries. Their structural presence does not prove cross-node membership or satisfaction semantics."
    }
  },
  "$defs": {
    "bounded_reference": {
      "type": "string",
      "minLength": 1,
      "maxLength": 2048
    },
    "continuation_envelope_reference": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "reference",
        "authority_boundary",
        "admission_control",
        "drift_check",
        "stop_conditions",
        "convergence_rule"
      ],
      "properties": {
        "reference": {
          "$ref": "#/$defs/bounded_reference",
          "description": "Reference to the externally owned continuation envelope."
        },
        "authority_boundary": {
          "$ref": "#/$defs/bounded_reference",
          "description": "Reference to the authority boundary that already governs continuation; this field does not create or widen that authority."
        },
        "admission_control": {
          "$ref": "#/$defs/bounded_reference",
          "description": "Reference to the admitted milestones and next-admission rule; unreferenced work is not admitted."
        },
        "drift_check": {
          "$ref": "#/$defs/bounded_reference",
          "description": "Reference to the external scope, authority, and evidence drift check."
        },
        "stop_conditions": {
          "type": "array",
          "minItems": 1,
          "maxItems": 100,
          "items": {
            "$ref": "#/$defs/bounded_reference"
          },
          "uniqueItems": true,
          "description": "References to explicit conditions that stop continuation and return control to the named authority."
        },
        "convergence_rule": {
          "$ref": "#/$defs/bounded_reference",
          "description": "Reference to the external convergence or bounded-handoff rule."
        }
      },
      "description": "A bounded set of references to externally owned continuation controls. These references are descriptive only: they do not execute checks, admit milestones, continue work, decide completion, or grant authority."
    },
    "source_authority": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "identity",
        "reference"
      ],
      "properties": {
        "identity": {
          "type": "string",
          "minLength": 1,
          "description": "Identity of the external authority source."
        },
        "reference": {
          "type": "string",
          "minLength": 1,
          "description": "Repository-neutral reference to the external authority record."
        }
      },
      "description": "Identifies existing source authority; it neither grants authority nor overrides project authority."
    },
    "node": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "role",
        "depends_on",
        "permission",
        "verifier",
        "output_contract",
        "resources",
        "classification"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "role": {
          "type": "string",
          "minLength": 1
        },
        "depends_on": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Declared dependency identities. JSON Schema does not prove that references exist, that the dependency graph is acyclic, or that external-gate topology is safe."
        },
        "permission": {
          "type": "string",
          "enum": [
            "READ_ONLY_REVIEW",
            "WRITE_AUTHORIZED",
            "CORRECTIVE_AUTHORIZED",
            "ENVIRONMENT_WRITE_AUTHORIZED",
            "SUBMIT_AUTHORIZED"
          ],
          "description": "An explicit permission mode recorded from the external authority envelope. It is not inferred from governance, is not granted by this schema, and cannot be upgraded by the node."
        },
        "verifier": {
          "type": "string",
          "minLength": 1,
          "description": "Reference to externally defined verification; it is not executed by this schema."
        },
        "output_contract": {
          "const": "urn:sagekit:graph-contract:v1:node-result",
          "description": "Stable Node Result v1 envelope identity."
        },
        "resources": {
          "type": "array",
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true,
          "description": "Declared resource identities; declarations do not acquire or mutate resources."
        },
        "classification": {
          "type": "string",
          "enum": [
            "required",
            "optional"
          ],
          "description": "Whether the node is required or optional; join satisfaction requires external semantic evidence."
        }
      }
    },
    "join": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "requires",
        "policy"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1
        },
        "requires": {
          "type": "array",
          "minItems": 1,
          "items": {
            "type": "string",
            "minLength": 1
          },
          "uniqueItems": true
        },
        "policy": {
          "type": "string",
          "enum": [
            "all-required",
            "required-plus-optional",
            "first-success",
            "manual-gate",
            "corrective-join"
          ],
          "description": "all-required is intended to preserve required nodes; required-plus-optional permits unavailable optional evidence only with a recorded reason; first-success is only for redundant, equivalent, read-only exploration and may not bypass required failure, authority, safety, approval, or acceptance. manual-gate records a required named human decision without making it; corrective-join records a finding/correction/re-review boundary. These are semantic requirements, not facts enforced by this structural schema."
        }
      }
    }
  },
  "allOf": [
    {
      "if": {
        "required": [
          "autonomy_level"
        ],
        "properties": {
          "autonomy_level": {
            "const": "goal-based"
          }
        }
      },
      "then": {
        "required": [
          "completion_verifier",
          "continuation_envelope"
        ]
      }
    }
  ]
}
